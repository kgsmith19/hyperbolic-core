# Nothing is public by default. Uncomment ONLY the location blocks below for
# paths the owner has explicitly decided to expose via Cloudflare Tunnel +
# Access (#168-#170) -- leave everything else commented.
#
# Each target must mirror nginx.conf/private_spa_locations.conf's private
# application target for the same path. edge-origin.test.mjs enforces this
# even while these blocks remain commented, so public policy cannot silently
# proxy a path to a different service.

# location / {
#     root /home/deploy/shell/current;
#     try_files $uri $uri/ /index.html;
# }

# location /life/ {
#     alias /home/deploy/lifeos-ui/current/;
#     try_files $uri $uri/ /life/index.html;
# }

# location /life/api/ {
#     proxy_pass http://127.0.0.1:8000;
# }

# location /api/ {
#     proxy_pass http://127.0.0.1:8200;
# }

# location /api/brain/ {
#     proxy_pass http://127.0.0.1:8100;
# }
