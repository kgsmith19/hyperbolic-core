# Nothing is public by default. Uncomment ONLY the location blocks below for
# paths the owner has explicitly decided to expose via Cloudflare Tunnel +
# Access (#168-#170) -- leave everything else commented.
#
# Each target must mirror docs/ops/tailscale-serve-apply.sh's mount -> target
# mapping exactly (docs/ops/edge-origin.test.mjs enforces this on every
# commit, including against the commented-out form below, so this file drifts
# loudly, not silently, if the private route table ever changes).

# location / {
#     root /home/deploy/shell/current;
#     try_files $uri $uri/ /index.html;
# }

# location /life/ {
#     alias /home/deploy/lifeos-ui/current/;
#     try_files $uri $uri/ /life/index.html;
# }

# location /life/api/ {
#     proxy_pass http://127.0.0.1:8000;
# }

# location /api/ {
#     proxy_pass http://127.0.0.1:8200;
# }

# location /api/brain/ {
#     proxy_pass http://127.0.0.1:8100;
# }
