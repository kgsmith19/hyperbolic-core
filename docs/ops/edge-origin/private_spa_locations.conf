# Minimal static routing for the private origin. Issue #346 replaces these
# simple directory semantics with the tested SPA fallback/asset policy.
location /life/ {
  alias /home/deploy/lifeos-ui/current/;
  index index.html;
}

location / {
  root /home/deploy/shell/current;
  index index.html;
}
