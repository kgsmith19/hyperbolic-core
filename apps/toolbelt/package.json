{
  "name": "toolbelt",
  "private": true,
  "description": "Toolbelt monorepo root: tool.json manifest contract and its validator (m3-01).",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "manifests:check": "node scripts/validate-manifests.mjs",
    "migrations:check": "node scripts/validate-migrations.mjs",
    "migrations:stage": "node scripts/stage-migrations.mjs"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1"
  }
}
