---
title: Test Justification Ledger
status: historical
created: 2026-08-07
updated: 2026-08-11
owner: Kyle
---

# Historical Test Evidence Ledger

> This file preserves evidence for tests and decisions that already shipped. It is not an active process requirement: new work does not require a spec, a ledger row, mutation bookkeeping, or a separate Test Review. Track future work in GitHub Issues and keep executable tests accurate.

---

## 1. Recorded tests

| Test ID | Name / location | Level | Traces to | Failure mode caught | Why not cheaper | Why not duplicate | Mutation verified | Runtime (ms) | Deletion criterion | Added |
|---|---|---|---|---|---|---|---|---|---|---|
| T-A-001 | `tests/skeleton.test.mjs` — **amended 2026-08-07, SPEC-0002 7.3 (declared, never silent):** "T-A-001 and T-I-003 re-insert fixed titles (`Spec Author`, `rls probe`) every run; under the unique index the second run collides. Amended flow, same contracts: `POST`; on `409`, `PATCH` the body by title instead. T-A-001's round-trip read-back stays byte-identical (its AC unchanged) and now also exercises the FR-003 update path on re-runs. The tests were correct for SL-000's world; the world legitimately changed — recorded here, not silently." | acceptance | AC-001, PROP-002 -> FR-001 | A saved body comes back altered (token, fence, newline, or non-ASCII corrupted), so every later render transforms corrupted input | Only a round-trip through the real API and storage can catch encoding loss | No other test reads a saved body back | 2026-08-07 (oracle mutation: expected body changed to fixture+`y`; red naming the exact diff, green on revert) | 800 | Never; the storage contract every slice assumes | SL-000 |
| T-I-001 | `tests/skeleton.test.mjs` | integration | AC-002, AC-003, PROP-003 -> FR-001 | A prompt outside FR-001's bounds (empty title, 201-char title, 100,001-char body) is stored | The `CHECK` is the cheap mechanism; this proves it is wired and survives future migrations | No other test sends invalid input | 2026-08-07 (dropped `prompt_title_check` via migration; red with actual `201` for an out-of-bounds title; constraint restored, the escaped empty-title row deleted) | 700 | FR-001's bounds change in the historical requirements | SL-000 |
| T-I-002 | `tests/skeleton.test.mjs` | integration | AC-004 -> NFR-003 | An unauthenticated caller reads prompt data (DR-002 is confidential) | RLS is database-level; only a real unauthenticated call proves it is switched on | No other test calls without a token | 2026-08-07 (owner policy replaced by `using (true)` via migration; red as anon received rows; policy restored, confirmed by `pg_policy` query) | 300 | Never while DR-002 is confidential | SL-000 |
| T-I-003 | `tests/skeleton.test.mjs` — **amended 2026-08-07, SPEC-0002 7.3 (declared, never silent):** "T-A-001 and T-I-003 re-insert fixed titles (`Spec Author`, `rls probe`) every run; under the unique index the second run collides. Amended flow, same contracts: `POST`; on `409`, `PATCH` the body by title instead. T-I-003's isolation assertions are unchanged. The tests were correct for SL-000's world; the world legitimately changed — recorded here, not silently." | integration | AC-005 -> NFR-003 | One user reads another user's prompts | Owner-scoped policy needs two real sessions; the positive control prevents a vacuous pass | No other test uses two identities | 2026-08-07 (same open-policy mutation; red as user B received user A's row; restored) | 900 | Tool becomes deliberately multi-user (historical requirements OOS-001 revisit) | SL-000 |
| T-A-002 | migration round-trip drill (manual, not executable) | acceptance | AC-006 -> CON-003 | The down migration strands the schema, table, or PostgREST exposure list | Only running the actual down proves rollback | Nothing else exercises the down | 2026-08-07 (down run against the real project: schema gone, `pgrst.db_schemas` back to exactly `public, core, idea`, confirmed by query; up re-applied, suite 4/4 green; DDL half also rehearsed on `lifeos-test` beforehand) | 0 (manual drill) | Never | SL-000 |
| T-U-001 | `tests/search.test.mjs` `matches_on_title_or_body_case_insensitively` | unit | AC-001, AC-002, PROP-002 -> FR-006 | A prompt that matches the search is hidden, or one that does not match is shown | Pure function; unit is the cheapest level | First test of this function | 2026-08-07 (case-fold removed from both sides: `SPEC` matched nothing; red expected `['Bug Fixer','Spec Author']` actual `[]`; reverted) | 3 | FR-006 changes shape (tags, SL-006) — then extended, not deleted | SL-001 |
| T-U-002 | `tests/search.test.mjs` `ranks_title_match_above_body_only_match` | unit | AC-001, PROP-004 -> FR-006 | A body-only match ranks above a title match | Ordering is logic, not wiring | T-U-001 asserts membership, this asserts order | 2026-08-07 (rank sort removed — one pass in input order; only this test red: actual `['Bug Fixer','Spec Author']` vs expected `['Spec Author','Bug Fixer']`; reverted) | 1 | Same as T-U-001 | SL-001 |
| T-U-003 | `tests/search.test.mjs` `returns_no_prompts_for_query_matching_nothing` | unit | AC-003 -> FR-006 | A no-match query returns rows anyway | Pure function; unit is the cheapest level | Only no-match case | 2026-08-07 (query ignored — `q` forced to `""` so everything matches; red expected `[]` actual all 3 fixture rows; reverted) | 1 | Same as T-U-001 | SL-001 |
| T-U-004 | `tests/search.test.mjs` `clearing_the_search_restores_all_prompts_unchanged` | unit | AC-004, PROP-003 -> FR-006 | Filtering mutates the list or loses rows on clear | Pure function; unit is the cheapest level | Only clear/identity case | 2026-08-07 (in-place `splice` filter returning the input; red — verified in isolation too: the cleared call was missing `Daily Journal`; reverted) | 1 | Same as T-U-001 | SL-001 |
| T-U-005 | `tests/search.test.mjs` `treats_metacharacter_and_non_ascii_queries_as_literal_text` | unit | PROP-001 -> FR-006 | A regex-metacharacter or non-ASCII query crashes or misfilters | Pure function; this is where literal-vs-pattern bugs live | Only adversarial-input case | 2026-08-07 (query compiled as `new RegExp(query, "i")`; only this test red: SyntaxError `Unterminated group` on the `(` probe; reverted) | 2 | Same as T-U-001 | SL-001 |
| T-I-004 | `tests/versions.test.mjs` | integration | AC-001, PROP-001 -> FR-002 | Two prompts share a title (case-folded), so title-as-key (FR-013) becomes ambiguous | The unique index is the mechanism; this proves it is wired through the real API | Only duplicate-title test | 2026-08-07 (integrator: dropped `prompt_title_unique` via migration; red — duplicate insert `201` not `409`; index restored after deduping rows the mutation window let collide) | 500 | FR-002 changes | SL-004 |
| T-I-005 | `tests/versions.test.mjs` | integration | AC-002, PROP-004 -> FR-003 | An insert records no version, so history starts at the first edit and the original is unrecoverable | Trigger behavior needs the real database | Only insert-version test | 2026-08-07 (integrator: dropped the `record_version` trigger via migration; red; trigger restored) | 280 | FR-003 changes | SL-004 |
| T-A-003 | `tests/versions.test.mjs` | acceptance | AC-003, PROP-002, PROP-003 -> FR-003 | An edit overwrites history (version 1 lost or altered) — UC-004's exact loss | End-to-end through the API is the AC as written | Only update-version test | 2026-08-07 (integrator: same trigger-drop mutation as T-I-005; red; restored) | 400 | FR-003 changes | SL-004 |
| T-I-006 | `tests/versions.test.mjs` | integration | AC-004, PROP-002 -> NFR-005 | A stored version is changed or deleted after the fact | Grant/policy absence is the mechanism; this proves both probes are rejected | Only immutability test | 2026-08-07 (integrator: granted `UPDATE` + an owner policy on `prompt_version` via migration; red; grant and policy reverted) | 630 | NFR-005 changes | SL-004 |
| T-I-007 | `tests/versions.test.mjs` | integration | PROP-003 -> FR-003 | A no-op body update (same value) appends a spurious version — PROP-003's "per body change" broken by "per body-update statement" | The trigger's distinct-body guard is the mechanism; only a live `UPDATE` proves the column-list trigger's actual firing semantics | No other test issues a same-value update | 2026-08-07 (integrator, added mid-review: the guard's own removal survived the full 8-test suite green — a real coverage hole GATE-TEST-JUSTIFIED J6 exists to catch. Guard removed via migration; new test red — `version_no` 2 present where 1 was expected; guard restored, suite 9/9 green) | 260 | Never; this is the invariant the trigger exists to hold | SL-004 |
| T-U-006 | `tests/render.test.mjs` `substitutes_a_single_token_with_its_value` | unit | AC-001, PROP-003 -> FR-004 | A token is left unsubstituted or substituted with the wrong value | Pure function; unit is cheapest | First test of `render` | 2026-08-07 (TOKEN_RE widened to `/\{\{([A-Z_][A-Z0-9_]*)\}\}./g`, swallowing one trailing char; red — expected `Repo is toolbelt.` actual `Repo is toolbelt`; reverted) | 2 | FR-004 changes shape | SL-002 |
| T-U-007 | `tests/render.test.mjs` `rejects_render_and_names_exactly_the_missing_variables` | unit | AC-002, PROP-001 -> FR-010 | A render with missing variables proceeds instead of blocking, or names the wrong variable; or a malformed token (`{{}}`, unterminated `{{`) crashes render instead of passing through as non-token text (RISK-001) | Pure function | Only missing-variable and malformed-token case | 2026-08-07 (missing-check forced to `[]`, every token treated optional; red — expected `ok:false` actual `ok:true`; reverted) | 2 | Same | SL-002 |
| T-U-008 | `tests/render.test.mjs` `substitutes_every_occurrence_and_extracts_a_repeated_token_once` | unit | AC-004, PROP-002 -> FR-004 | A repeated token is substituted only once, or extracted twice | Pure function | Only repeated-token case | 2026-08-07 (replace() regex lost the `/g` flag, only first occurrence substituted; red — expected `toolbelt and toolbelt again` actual `toolbelt and {{REPO}} again`; reverted) | 1 | Same | SL-002 |
| T-U-009 | `tests/render.test.mjs` `passes_a_token_free_body_through_unchanged` | unit | AC-005 -> FR-004 | A body with no tokens is altered, or an empty panel is shown | Pure function | Only token-free case | 2026-08-07 (none of the mutations tried for T-U-006..012/016 turned this one red — an empty body has no token span for a substitution/order bug to touch — so a targeted mutation was added: `text: text \|\| "empty"`, falsy-string coalescing; red — expected `''` actual `'empty'`; reverted) | 1 | Same | SL-002 |
| T-U-010 | `tests/render.test.mjs` `never_mutates_its_body_or_values_arguments` | unit | PROP-004 -> FR-004 | `render` mutates its inputs | Pure function | Only mutation-check case | 2026-08-07 (`values.__mutated = true` added before substitution; red — values object gained a key vs its snapshot; reverted. Also went red, for a different reason, under the first-occurrence-only and positional-substitution mutations below, since both alter `result.text`) | 1 | Same | SL-002 |
| T-U-011 | `tests/render.test.mjs` `passes_text_adjacent_to_a_token_through_byte_for_byte` | unit | PROP-005 -> FR-004 | Text adjacent to a token (including a section-fence-shaped string, non-ASCII) is altered | Pure function; this is the SL-003 forward-compat guard | Only adjacency case | 2026-08-07 (TOKEN_RE widened to `/\{\{([A-Z_][A-Z0-9_]*)\}\}./g`, swallowing one trailing char; red — the `!` immediately after `{{NAME}}` was eaten; reverted) | 1 | Never — the guarantee SL-003 will build on | SL-002 |
| T-U-012 | `tests/render.test.mjs` `output_is_unaffected_by_the_key_order_of_values` | unit | PROP-006 -> FR-004 | Output depends on `values` key order | Pure function | Only order case | 2026-08-07 (substitution made positional via `Object.values(values)` order instead of by-name lookup; red — forward-order text `x-y` vs reverse-order text `y-x`, `assert.equal(forward.text, reverse.text)` failed; reverted. Also went red under the trailing-char boundary mutation above) | 1 | Same | SL-002 |
| T-U-016 | `tests/render.test.mjs` `extracts_and_reports_missing_variables_in_first_occurrence_order` | unit | AC-004 (extends), PROP-002 -> FR-004 | `extractVariables` (and, through it, a multi-name `missing` list) does not preserve first-occurrence order — a coverage hole T-U-008's single-distinct-name fixture could not catch, found during SL-002 mutation verification (the historical test practice: "if no mutation turns any test red, you have a hole") | Pure function; unit is cheapest | T-U-008 only exercises one distinct name (dedup), never order across 3+ distinct names | 2026-08-07 (`names.push` changed to `names.unshift`, reversing first-occurrence order; red — expected `['C','A','B']` actual `['B','A','C']`; this was the mutation that motivated adding T-U-016 in the first place, since no other test caught it; reverted) | 1 | Same as T-U-008 | SL-002 |
| T-I-008 | `tests/tags.test.mjs` `saves_tags_lowercased_and_deduplicated` | integration | AC-001, PROP-002, PROP-005 -> FR-012 | Tags saved with mixed case or duplicates produce more than the deduplicated, lowercased set | DB round-trip needed to prove storage, not just client-side dedup | Only tag-save test | 2026-08-07 (integrator: dropped the `owner_select` policy entirely via migration; red — `PGRST116`/empty read-back where a row was expected; policy restored) | 400 | FR-012 changes | SL-006 |
| T-I-009 | `tests/tags.test.mjs` `filters_to_only_prompts_carrying_the_tag` | integration | AC-002, PROP-003 -> FR-012 | A filter shows a prompt lacking the tag, or hides one that has it | Real query against real rows | Only filter test | 2026-08-07 (integrator: same policy-drop mutation as T-I-008; red — the filter query returned zero rows for either tag; policy restored) | 350 | FR-012 changes | SL-006 |
| T-I-010 | `tests/tags.test.mjs` `second_click_on_same_tag_clears_the_filter` — **reclassified unit-level, SPEC-0004 §11 (declared, never silent):** filtering is client-side over an already-fetched prompts+tags list (no server call on chip click), so the cheapest sufficient mechanism proving "second click clears" is a pure-function test of the toggle decision (`toggleTagFilter`, `web/search.mjs`), not a DB round trip. Spec's original integration framing is preserved below for traceability | unit (spec declared integration; see note) | AC-005 -> FR-012 | The filter fails to clear on a second click | Pure function; no server call exists in the toggle path to make a DB round trip meaningful | Only clear-filter test | 2026-08-07 (worktree, no DB needed: `toggleTagFilter` reverted to the pre-implementation stub `(current, tag) => tag`, which never clears; red — expected `null` actual `"sdd"`; reverted to the ternary) | 1 | FR-012 changes | SL-006 |
| T-I-011 | manual integrator drill (like T-A-002), not executable in the anon-key suite | integration | PROP-001 -> FR-012 | A cascade-deleted prompt leaves orphan tag rows | Cascade behavior is a real FK property, needs the real database | No other test deletes a prompt (no delete grant exists on `prompt.prompt` either — this test uses the integrator's elevated access to delete for the purpose of proving the FK, recorded as an integrator-only drill, not a suite member) | 2026-08-07 (drilled against the real project via elevated access: inserted a prompt + a tag row, deleted the prompt, confirmed 0 orphan tag rows by query — both rehearsed on `lifeos-test` first and re-run on prod) | 0 (manual drill) | Never | SL-006 |
| T-I-014 | `tests/tags.test.mjs` `rejects_tag_over_100_chars_with_23514` | integration | AC-006, PROP-001 -> FR-012 | A tag outside the length bound is stored | The `CHECK` is the cheap mechanism; this proves it is wired | Only over-length-tag test | 2026-08-07 (integrator: dropped `tag_tag_check` via migration; red with actual `201` for a 101-char tag; constraint restored, the escaped row deleted) | 320 | FR-012's bound changes | SL-006 |
| T-U-013 | `tests/search.test.mjs` `finds_tag_only_match_absent_from_title_and_body` | unit | AC-003, PROP-001 -> FR-006 | A tag-only match is absent from search results | Pure function once tags are loaded alongside prompts | Extends T-U-001 without duplicating it (title/body already covered there) | 2026-08-07 (worktree, no DB needed: tag branch removed from `searchPrompts`, pre-tags behavior restored; red — expected `['Daily Journal']` actual `[]`; reverted) | 1 | FR-006 changes shape | SL-006 |
| T-U-014 | `tests/search.test.mjs` `ranks_title_above_tag_above_body_only_match` | unit | AC-004, PROP-004 -> FR-006 | Tag match ranks above or below the wrong group | Pure function; ordering is logic | Extends T-U-002 | 2026-08-07 (worktree, no DB needed: tag group folded into `bodyMatches` instead of its own rank; red — expected `['sdd in Title','Tag Only','Body Only']` actual `['sdd in Title','Body Only','Tag Only']`; reverted) | 1 | FR-006 changes shape | SL-006 |
| T-I-012 | `tests/restore.test.mjs` | integration | AC-001 -> FR-009 | The version-history panel would show a history missing an entry, out of order, or with fabricated/duplicate timestamps | Needs three real trigger-written rows across real body changes; only a live `GET` proves ordering and distinct timestamps | No other test lists a full `prompt_version` history for display | 2026-08-07 (worktree: no new application code on this call path — three real API calls exercise only SL-004's already-verified trigger, so per the historical test practice the mutation targets the test's own discriminating assertion, T-A-001's precedent. Expected order flipped to `[1, 2, 3]`; red — actual `[3, 2, 1]`; reverted, suite green) | 528 | Never; this is the data the history panel reads directly | SL-008 |
| T-A-004 | `tests/restore.test.mjs` | acceptance | AC-002, PROP-002, PROP-003 -> FR-009 | A restore loses version 1, corrupts it, or the new version's body doesn't match the source exactly | End-to-end through the real API is the AC as written | Only test that restores an old version as a new current version | 2026-08-07 (worktree: no new application code on this call path — reuses SL-004's trigger/grant unchanged, so the mutation targets the assertion. Expected version-4 body changed to `"third body"`; red — actual `"original body"`; reverted, suite green) | 598 | Never; this is UC-004's exact guarantee | SL-008 |
| T-I-013 | `tests/restore.test.mjs` | integration | AC-003, PROP-004 -> FR-009 | A restore whose target body already equals the current body spuriously creates a new version | Real trigger behavior; proves the restore call path composes with SL-004's existing distinct-body guard rather than fighting it | No other test restores into an already-matching state | 2026-08-07 (worktree: no new application code on this call path — composes SL-004's guard (T-I-007) through a new sequence only, so the mutation targets the assertion. Expected count changed to `before.length + 1`; red — actual unchanged count; reverted, suite green) | 545 | Never; documents the guard's exact boundary | SL-008 |
| T-U-015 | `tests/restore.test.mjs` (`web/restore.mjs` `isCurrentVersion`) | unit | AC-004 -> FR-009 | The UI offers a restore control for the version that is already current | Pure function; cheapest mechanism, no DOM needed for the decision itself | Only this decision's logic | 2026-08-07 (worktree: real production code mutated — `isCurrentVersion`'s `===` inverted to `!==` in `web/restore.mjs`; red — expected `true` actual `false`; reverted, suite green) | 1 | FR-009 changes | SL-008 |
| T-I-015 | `tests/restore.test.mjs` | integration | AC-005, NFR-003 -> FR-009 | A restore reaches across ownership and modifies another user's prompt | RLS is database-level; only a real second identity proves it holds on this specific call path (restore) | No other test in this file uses two identities; SL-000's T-I-003 proves the general case, this proves restore composes with it | 2026-08-07 (worktree: no new application code on this call path — the restore button issues the same `PATCH` any caller could send, ownership is correctly never checked client-side, so RLS enforcement is unchanged from SL-000/SL-004 and the mutation targets the assertion, two ways: (a) expected empty array changed to `[{ id }]`; red — actual `[]`; reverted; (b) expected post-attempt count changed to `3`; red — actual `2`; reverted, suite green) | 548 | Never while ownership is per-row | SL-008 |
| T-U-017 | `tests/sections.test.mjs` `excludes_an_unlisted_section_and_strips_every_fence` | unit | AC-001, PROP-003, PROP-007 -> FR-005 | An excluded section's content survives into the copied prompt, an included section is lost, or raw `<!--OPTIONAL` fence text leaks into text pasted at an agent | Pure function; unit is cheapest | The literal FR-005 criterion; first test of exclusion | 2026-08-07 (M1, real production code: `applySections`' ternary replaced by bare `content`, so nothing is ever excluded; red — `BBB` present where the AC forbids it; reverted) | 1 | FR-005 changes shape | SL-003 |
| T-U-018 | `tests/sections.test.mjs` `includes_every_listed_section_in_source_order_without_fences` | unit | AC-002, PROP-003 -> FR-005 | Including every section leaves fence comments behind, or reorders content | Pure function | T-U-017 excludes; this includes | 2026-08-07 (M2: ternary replaced by bare `""`, so every section is dropped; red — expected `head AAA mid BBB tail` actual `head  mid  tail`; reverted) | 1 | Same as T-U-017 | SL-003 |
| T-U-019 | `tests/sections.test.mjs` `extracts_section_ids_in_first_occurrence_order_deduplicated` | unit | AC-003, PROP-002 -> FR-005 | `extractSections` misses an id, invents one, duplicates a repeated id, or loses first-occurrence order — the checkbox list and (in SL-005) the saved include list both read this | Pure function | Only extraction test | 2026-08-07 (M3: `ids.push` changed to `ids.unshift`, reversing first-occurrence order — the same mutation that exposed SL-002's T-U-016 hole; red — expected `['a','b']` actual `['b','a']`; reverted) | 2 | Same as T-U-017 | SL-003 |
| T-U-020 | `tests/sections.test.mjs` `treats_malformed_fences_as_literal_text` | unit | AC-004, PROP-001, PROP-005 -> FR-005 | A malformed fence (unterminated, mismatched ids, empty id) is treated as a section, is mangled, or crashes render | Pure function; this is where parser-boundary bugs live | Only adversarial-fence case; guards SL-002's T-U-011 contract from the other side | 2026-08-07 (**this test passed against the R2 stub** — a no-op trivially satisfies "does not transform", which is inherent to an invariance test, so the mutation is the only thing that proves it earns its keep, SPEC-0006 §11. M4: the closing fence's backreference `\1` replaced by `[A-Za-z0-9_-]*` and the id charset `+` loosened to `*`, so a mismatched-id pair parses as a section; red — expected `<!--OPTIONAL:x-->body<!--/OPTIONAL:y-->` actual `body`; reverted) | 2 | Never — the SL-002 forward-compat guarantee this slice inherited | SL-003 |
| T-U-021 | `tests/sections.test.mjs` `does_not_demand_a_variable_that_lives_only_in_an_excluded_section` | unit | AC-005 -> FR-005, FR-010 | A variable inside an excluded section blocks the render (the user is asked to fill in text that is about to be deleted), or one in a kept section stops being demanded | Pure function | Only FR-005 x FR-010 interaction test | 2026-08-07 (M5: `extractVariables(kept)` reverted to `extractVariables(body)`, i.e. variables resolved before sections — the exact ordering bug this test exists to pin; red — expected `ok:true` actual `ok:false` naming `GONE`; reverted. Also confirmed live in the browser drill, SPEC-0006 DoD) | 1 | FR-005 or FR-010 changes | SL-003 |
| T-U-022 | `tests/sections.test.mjs` `is_pure_and_unaffected_by_include_order_or_a_fence_free_body` | unit | PROP-004, PROP-006, AC-006 -> FR-005 | `render` mutates its arguments, output depends on `includes` order, or a fence-free body is altered by the new third argument (SL-002 regression) | Pure function | Only purity/order/regression case | 2026-08-07 (**also passed against the R2 stub**, same invariance-test reasoning as T-U-020. M6: membership test `includes.includes(id)` narrowed to `includes[0] === id`, making the result depend on the order of the include list; red — the `['b','a']` and `['a','b']` renders no longer matched; reverted) | 1 | Same as T-U-017 | SL-003 |
| T-U-023 | `tests/sections.test.mjs` `adding_an_id_to_includes_only_adds_content` | unit | PROP-008 -> FR-005 | Adding an id to `includes` removes content instead of adding it | Pure function; monotonicity is logic | Only monotonicity case; the first slice where PROP-008 is non-vacuous | 2026-08-07 (M7: ternary inverted to `includes.includes(id) ? "" : content`, so listing an id *removes* its content; red — expected `''`/`'A'`/`'AB'`/`'ABC'` actual the exact complement; reverted) | 1 | Same as T-U-017 | SL-003 |
| T-U-024 | `tests/performance.test.mjs` `renders_a_realistic_maximum_size_body_within_budget` | unit | AC-001 -> NFR-002 | A realistic 100,000-char body (FR-001's ceiling) becomes slow enough for the user to feel it on every copy | Timing is the only way to observe timing; no static check proves a wall-clock budget | Only realistic-body benchmark | 2026-08-07 (M2, targeted: a bounded O(n^2) loop injected into `applySections` -- a perf test's only meaningful mutation is a slowdown; red with p95 222.2ms vs the 100ms budget; reverted) | 130 | NFR-002's threshold changes | SL-010 |
| T-U-025 | `tests/performance.test.mjs` `renders_a_pathological_fence_body_within_budget` | unit | AC-002, PROP-001 -> NFR-002 | A body the schema explicitly permits (99,994 chars of unterminated `<!--OPTIONAL:a-->`) takes ~210 ms — 2.1x NFR-002's budget. This is the defect the slice exists to fix, introduced by SL-003's backtracking pair-regex | Same | Only pathological-body benchmark | 2026-08-07 (M1, the real pre-fix parser restored from `origin/main`: red with p95 210.9ms vs 100ms. Run in ISOLATION deliberately -- in-suite, T-U-024 runs first and warms V8's regex JIT, which drops this body from 164.8ms to 29.7ms and would have hidden the defect. That 5.5x JIT swing is what produced the retracted 34.7ms measurement; see SPEC-0007 section 11) | 45 | Same as T-U-024 | SL-010 |
| T-U-026 | `tests/performance.test.mjs` `section_parsing_grows_at_most_linearly` | unit | AC-003, AC-005, PROP-007, PROP-008 -> NFR-002 | Section parsing regresses to superlinear again — which a fixed wall-clock threshold would miss entirely on fast hardware, since O(n^2) at small n still looks fast. Also asserts AC-005 (interleaved sections stay non-overlapping and never corrupt) | A growth ratio is machine-independent where an absolute threshold is not | T-U-024/025 pin the absolute budget; this pins the shape of the curve | 2026-08-07 (M1, same pre-fix parser. The test verbatim cannot run against it -- 60,000 fences quadratic is ~17s per render, ~4min for the test, which is itself the defect -- so the ratio was measured at 1250 vs 5000 fences: quadratic grows 15.0x, linear 0.9x, against the test's 8x threshold. Discriminates cleanly) | 75 | Section parsing is removed | SL-010 |
| T-A-005 | `tests/usage.test.mjs` | acceptance | AC-001, PROP-011 -> FR-011 | A prompt copied twice does not produce two usage rows with distinct timestamps -- the literal FR-011 acceptance criterion | End-to-end through the real API is the AC as written | Only test that reads back a usage count | 2026-08-07 (dropped policy `owner_select` on `prompt.usage`; both tests in the file went red, `403` instead of the expected status -- `Prefer: return=representation` needs read-back permission on any write, so this is load-bearing for both write paths, not just reads; policy restored) | 964 | Never; this is FR-011's own stated guarantee | SL-007 |
| T-I-016 | `tests/usage.test.mjs` | integration | AC-002, PROP-012 -> FR-011 | A usage row is stored naming a version that was never actually created for that prompt | The composite FK (`prompt_id, version_no`) is the mechanism; this proves it is wired | Only test that writes an out-of-range `version_no` | 2026-08-07 (dropped the composite FK constraint via `ALTER TABLE ... DROP CONSTRAINT`; red -- `201` with a real row for `version_no=2` on a version-1-only prompt, instead of `409`; escaped row deleted, constraint restored) | 352 | Never; this keeps usage history honest | SL-007 |
| T-A-006 | `tests/archive.test.mjs` | acceptance | AC-001 -> FR-014 | Archiving a prompt loses the row, loses a version, fails to hide it from the default view, or a reactivate fails to bring it back | End-to-end through the real API is the AC as written: archive, verify hidden + intact, reactivate, verify visible again | Only test that exercises the full archive-then-reactivate round trip | 2026-08-08 (revoked `update (is_active)` grant via migration; red -- `403`/`42501` on the archive `PATCH` instead of `200`; grant restored) | 2929 | Never; FR-014's own stated guarantee | SL-011 |
| T-I-017 | `tests/archive.test.mjs` | integration | AC-002 -> FR-014, FR-002 | Archiving a prompt silently frees its title, so a duplicate title-under-a-different-prompt would then succeed where FR-002 says it must not | Proves the deliberate scope decision (archiving does not touch `prompt_title_unique`) actually holds, not just documented | Only test that inserts a duplicate title against an archived prompt | 2026-08-08 (no new mechanism on this call path -- `prompt_title_unique` is already mutation-verified by T-I-004, so per this ledger's own T-I-012 precedent the mutation targets the test's own discriminating assertion: expected duplicate status changed to `201`; red -- actual `409`; reverted) | 423 | FR-002 or FR-014 changes | SL-011 |
| T-I-018 | `tests/archive.test.mjs` | integration | AC-003 -> FR-014, NFR-003 | A non-owner can archive another user's prompt through the new `is_active` column grant | RLS is database-level; only a real second identity proves the new column composes with the existing owner policy | No other test in this file uses two identities; SL-000's T-I-003 proves the general case, this proves it holds for `is_active` specifically | 2026-08-08 (two mutations: (a) revoked `update (is_active)` grant, red -- non-owner `PATCH` returned `403` where `200`/0-rows was expected, grant restored; (b) `owner_all` policy replaced with `using (true) with check (true)` via migration -- only this test went red among the three in the file, non-owner update actually applied `is_active=false`; policy restored) | 414 | Never while ownership is per-row | SL-011 |
| T-U-027 | `tests/search.test.mjs` `filterByActive` | unit | AC-001 -> FR-014 | Archived prompts leak into the default view, an active prompt is hidden by default, or the "show archived" case drops a prompt or reorders the list | Pure function; unit is the cheapest level | Only test of this decision | 2026-08-08 (real production code: the `showArchived ? prompts : prompts.filter(...)` filter replaced with `showArchived ? prompts : prompts`, a no-op; red -- default view returned all 3 prompts instead of the 2 active ones; reverted) | 0.14 | FR-014 changes shape | SL-011 |
| T-I-019 | `tests/configuration.test.mjs` | integration | AC-002 -> FR-008 | A saved configuration's `values` (jsonb) or `sections` (array) comes back altered on read -- reordered keys, coerced types, dropped entries | Only a round trip through the real API and storage proves jsonb/array columns preserve structure | First test of this table | 2026-08-08 (revoked `select` grant via migration; red -- both configuration tests failed to read back; grant restored) | 1666 | Never; the storage contract every later use of configurations assumes | SL-005 |
| T-I-020 | `tests/configuration.test.mjs` | integration | AC-003 -> FR-008, NFR-003 | A non-owner reads another user's saved configurations through the EXISTS-based ownership policy | Same policy shape as `prompt.tag` (SR-23), but RLS is evaluated per-table; only a live cross-identity call proves it is actually wired on `prompt.configuration` specifically | No other test in this file uses two identities | 2026-08-08 (two mutations: (a) revoked `select` grant, both tests red, restored; (b) `owner_select` policy opened to `using (true)` via migration -- only this test went red among the two in the file, discriminating the RLS predicate specifically; restored) | 420 | Never while ownership is per-row | SL-005 |
| T-U-028 | `tests/panel.test.mjs` `applyConfigValues` | unit | AC-001 -> FR-008 | A selected configuration's value for a still-present variable is not applied, or a value for a variable no longer in the body is applied anyway | Pure function; unit is the cheapest level; this is FR-008's own literal AC (`REPO=toolbelt` reads back on selection) | Only test of this decision | 2026-08-08 (real production code: `if (name in values)` guard removed, copying every key in `values` regardless of `names`; red -- stale `GONE` key present where the AC forbids it; reverted) | 1.6 | FR-008 changes shape | SL-005 |
| T-U-029 | `tests/performance.test.mjs` `searches_1000_prompts_within_budget` | unit | NFR-001 | Search becomes slow enough to feel at the library size the historical requirements names -- no production code changes with this row; `searchPrompts` is measured as it already is, same posture as NFR-002's benchmarks | Timing is the only way to observe timing | Only search benchmark; distinct from T-U-024..026, which measure `render`, not `searchPrompts` | 2026-08-08 (targeted mutation: a 20x-repeated `O(n log n)` sort injected inside the match loop; a single weaker O(n) probe first tried (70ms) did not clear the 300ms budget, so the mutation was strengthened until it did; red at p95 1161.5ms vs the 300ms budget at 1,000 prompts; reverted) | ~2 | NFR-001's threshold changes | doc-closure |
| T-U-030 | `tests/search.test.mjs` `shouldFocusSearch` | unit | NFR-008 | Pressing `/` while already typing in a text field steals focus and eats the character, or pressing `/` anywhere else fails to focus search | Pure function; unit is the cheapest level | Only test of this decision | 2026-08-08 (real production code: the `!TYPING_TAGS.includes(targetTagName)` guard removed; red -- `INPUT`/`TEXTAREA` cases returned `true` where the AC forbids it; reverted) | 0.1 | NFR-008 changes shape | doc-closure |
| T-A-007 | `tests/render-endpoint.test.mjs` | acceptance | AC-001 -> FR-013 | The RPC returns the wrong text, the wrong status, or the wrong content type -- FR-013's own AC as written (corrected to `application/json`, section 2.1) | End-to-end through the real API is the AC as written | Only test that checks content type | 2026-08-08 (revoked `execute` grant from `authenticated`; first attempt stayed green -- Postgres's default `PUBLIC` execute grant was still covering it, a real finding, section 2.1; `revoke ... from public` added, then the same revoke reddened all four tests in the file; grant restored) | 515 | Never; FR-013's own stated guarantee | SL-009 |
| T-I-021 | `tests/render-endpoint.test.mjs` | integration | AC-002 -> FR-013 | An unknown prompt name returns something other than `404` (a `500`, or a `200` with garbage) | The `PT404` error-code mapping is PostgREST's own convention; only a live call proves it is wired for this function | Only unknown-name test | 2026-08-08 (this test happened to pass vacuously before the migration existed too -- PostgREST returns `404 PGRST202` for a missing function, coincidentally the right status for the wrong reason. Real mutation: `errcode` changed from `PT404` to `P0001`; red -- `400` instead of `404`; reverted) | 259 | FR-013 changes shape | SL-009 |
| T-I-022 | `tests/render-endpoint.test.mjs` | integration | AC-003 -> FR-013, FR-010 | An unfilled variable renders silently as literal `{{NAME}}` text instead of being blocked -- FR-010's guarantee, reused server-side rather than re-decided | Real function execution; only a live call proves the guard fires | Only missing-variable test | 2026-08-08 (the missing-variable `array_agg`/`raise exception` block removed entirely; red -- `200` with the literal `{{REPO}}` leaked into the body instead of `422`; reverted) | 312 | FR-010 or FR-013 changes | SL-009 |
| T-I-023 | `tests/render-endpoint.test.mjs` | integration | AC-004 -> FR-013, NFR-003 | A non-owner's call reaches another user's prompt through the new RPC surface | `security invoker` (not `definer`) is the mechanism -- the function inherits the caller's own RLS, no new policy exists; only a live cross-identity call proves it actually composes | No other test in this file uses two identities; SL-000's T-I-003 proves the general RLS case, this proves it holds through an RPC call specifically | 2026-08-08 (this test also passed vacuously pre-migration, same `PGRST202` coincidence as T-I-021. Real mutation: `security invoker` changed to `security definer`; red -- user B successfully rendered user A's prompt, `200` instead of `404`, discriminating this test alone among the four; reverted) | 435 | Never while ownership is per-row | SL-009 |
| T-E-001 | `tests/e2e/critical-flow.test.mjs` | e2e | AC-001 (FR-001 save), AC-001 (FR-007 render/copy), AC-001 (FR-010 variable fill) -> Issue #18 | The save-render-copy flow breaks at the real browser level -- a sign-in failure, a save that doesn't appear in the list, a variable input that isn't rendered, or a clipboard that doesn't contain the right text -- while the integration suite passes at the API level | Only a real Chromium browser exercises the full DOM event stack, the `navigator.clipboard` API, and the combined sign-in → save → render → copy sequence; the integration suite tests each piece independently | Only E2E test; no other test drives the full sign-in → save → render → copy UI sequence | Sandbox environment limitation: headless Chromium in this sandboxed environment cannot reach the live Supabase host, so the E2E test's mutation verification is a declared gap (same posture as T-A-002). The discriminating assertion (clipboard text equality against `EXPECTED_RENDERED`) is structurally verified by inspection: changing `EXPECTED_RENDERED` to any other string would redden the test; the Playwright clipboard grant mechanism is the same path T-A-002's manual drill confirmed. This historical gap remains recorded; the browser journey now runs in `Toolbelt PR Gate`, so each pull request supplies current execution evidence. | ~30 000 (network-bound; outside the 120 s fast suite budget — now runs inside `.github/workflows/toolbelt-ci.yml` as part of `Toolbelt PR Gate`) | Issue #18 AC changes or the save/render/copy flow changes materially | Issue #18 |

## 2. Level distribution

| Level | Count | Target share | Total runtime | Notes |
|---|---|---|---|---|
| Unit (`T-U-`) | 31 | ~70% | ~285ms (T-U-024..026 and T-U-029 are benchmarks and dominate; still trivial against the 120s budget) | SL-001's `searchPrompts` (T-U-001..005): example probes over the SPEC-0001 PROP-001..004 domains. SL-002's `extractVariables`/`render` (T-U-006..012, T-U-016): example probes over SPEC-0003's ACs and PROP-001..006 domains. SL-006's `searchPrompts` tag matching (T-U-013..014) and `toggleTagFilter` (T-I-010, reclassified unit — see its row). SL-008's `isCurrentVersion` (T-U-015). SL-003's `extractSections`/`render` sections (T-U-017..023): SPEC-0006's AC-001..006 and PROP-001..008, including the first non-vacuous PROP-008 (monotonicity) in this repo. SL-011's `filterByActive` (T-U-027). SL-005's `applyConfigValues` (T-U-028). NFR-001's `searchPrompts` benchmark (T-U-029). NFR-008's `shouldFocusSearch` (T-U-030) |
| Property (in unit) | 1 | 1-3 per slice | 0.8s | PROP-002 round-trip inside T-A-001 |
| Integration (`T-I-`) | 21 | ~20% | ~9.6s | T-I-001..003 (SL-000), T-I-004..007 (SL-004), T-I-008/009/014 (SL-006, all green post-migration), T-I-012/013/015 (SL-008, restore composing with SL-004's trigger/grant/RLS), T-I-016 (SL-007, composite FK to `prompt_version`), T-I-017/018 (SL-011, archive composing with the title-unique index and owner RLS), T-I-019/020 (SL-005, configuration round trip and RLS, reusing `prompt.tag`'s ownership shape), T-I-021/022/023 (SL-009, the render RPC's 404/422/RLS composition) |
| Acceptance (`T-A-`) | 7 | 1 per AC | ~6.2s + drill | T-A-001, T-A-003, T-A-004, T-A-005, T-A-006, T-A-007 executable; T-A-002 a recorded drill |
| E2E (`T-E-`) | 1 | 1 thin journey | ~30s (`Toolbelt PR Gate`) | T-E-001: critical prompt flow (sign-in → save → render → copy). Runs inside `.github/workflows/toolbelt-ci.yml` on pull requests, merge groups, manual dispatches, and the daily schedule as part of `Toolbelt PR Gate`. |
| **Total (SL-000..SL-008, SL-010, SL-007, SL-011, SL-005, SL-009, Issue #18)** | 59 executable, all green under their respective runners | | ~5.8s Node suite + ~30s E2E inside Toolbelt PR Gate | Phase 0 cap (4) applied to SL-000 only; every later slice declares standard ceilings; well under 120 s for the fast suite |

## 3. Regression register

| Test ID | Defect | Date found | How it reached production | Root cause | Fixed in | Also fixed by a cheaper mechanism? |
|---|---|---|---|---|---|---|

## 4. Deleted tests

| Test ID | Name | Deleted | Reason | Replaced by |
|---|---|---|---|---|

## 5. Quarantine

| Test ID | Reason quarantined | Quarantined on | Expires | Owner | Resolution |
|---|---|---|---|---|---|

Quarantine is capped at 14 days.

## 6. Historical ledger self-check

- [x] Every test file has a matching row; every row's test exists. Fourteen files (`skeleton.test.mjs`, `search.test.mjs`, `versions.test.mjs`, `render.test.mjs`, `tags.test.mjs`, `restore.test.mjs`, `sections.test.mjs`, `performance.test.mjs`, `usage.test.mjs`, `archive.test.mjs`, `configuration.test.mjs`, `panel.test.mjs`, `render-endpoint.test.mjs`, `e2e/critical-flow.test.mjs`), fifty-nine executable rows, plus the T-A-002 and T-I-011 drills labelled as not executable.
- [x] Every `Traces to` resolves through an `AC`/`PROP` to an `FR`/`NFR`/`CON`.
- [x] Every row has a mutation-verified date and a deletion criterion. SL-007's T-A-005 and T-I-016 verified 2026-08-07, direct Supabase access (this session, not a worktree-constrained worker): the composite FK dropped via `ALTER TABLE ... DROP CONSTRAINT` reddened T-I-016 (`201` instead of `409` for an out-of-range `version_no`), constraint restored; the `owner_select` policy dropped reddened both tests (`403` — `Prefer: return=representation` needs read-back permission on any write, a secondary finding beyond T-A-005's own claimed mechanism), policy restored. Live browser drill (Chromium via the Node-relay technique, clipboard permissions granted explicitly) confirmed the real click-to-copy path writes a real row, and surfaced a genuine defect the automated tests alone would not have: 21 pre-SL-004 fixture prompts had zero `prompt_version` rows, which crashed the client's new version-embed query. Backfilled in the same migration (historical requirements 0.1.8, SPEC-0008 section 2.1). All SL-004 rows verified 2026-08-07 by the integrator (DDL mutations). SL-002's T-U-006..012 and T-U-016 verified 2026-08-07 in the worktree (pure-function mutations). SL-006's T-U-013, T-U-014, and T-I-010 verified 2026-08-07 in the worktree (pure-function mutations, no DB dependency); T-I-008, T-I-009, T-I-014 verified 2026-08-07 by the integrator after the migration was applied and rehearsed on `lifeos-test` first (DDL mutations: policy drop, `CHECK` drop); T-I-011 drilled live on the real project (insert, delete parent, confirm 0 orphans). SL-008's T-U-015 verified 2026-08-07 in the worktree (real production-code mutation, `isCurrentVersion`); T-I-012, T-A-004, T-I-013, T-I-015 verified 2026-08-07 in the worktree by mutating each test's own discriminating assertion (no new application code exists on any of their call paths — each composes SL-004's already-mutation-verified trigger/grant or SL-000's already-mutation-verified RLS through a new sequence, per the historical test practice's guidance not to manufacture mutations against reused, already-covered mechanisms). SL-003's T-U-017..023 verified 2026-08-07 by seven distinct mutations of real production code in `web/render.mjs` (M1..M7: never-exclude, always-exclude, reversed extraction order, dropped closing-fence backreference, variables-before-sections, order-dependent membership, inverted inclusion) — each turned its own test red and was reverted. Two of the seven (T-U-020, T-U-022) had passed against the R2 stub, which their rows record explicitly: both are invariance tests, whose whole claim is that certain input is *not* transformed, so a no-op satisfies them vacuously and only a mutation can prove they discriminate. SL-010's T-U-024..026 verified 2026-08-07: M1 restored the real pre-fix quadratic parser from `origin/main` (reddening T-U-025 at 210.9ms and T-U-026 at a 15.0x growth ratio), and M2 injected a bounded O(n^2) loop to redden T-U-024 at 222.2ms — a benchmark's only meaningful mutation being a slowdown. T-U-025 must be mutation-checked in isolation: V8 tiers up the section regex once it has matched real input, which swings that body between 164.8ms and 29.7ms and would otherwise mask the defect. Stability: the three benchmarks ran green 8/8 consecutive unit-suite runs. None `pending`.
- [x] Total suite runtime ~5.8s measured (`node --test "tests/*.test.mjs"`, 58/58 green), under 120 s. T-U-029 (NFR-001) verified 2026-08-08: a weaker O(n) probe (70ms) didn't clear the 300ms budget, so the mutation was strengthened to a 20x-repeated O(n log n) sort until it did (p95 1161.5ms), then reverted. T-U-030 (NFR-008) verified 2026-08-08: the typing-field guard removed, red on the `INPUT`/`TEXTAREA` cases, reverted. SL-009's T-A-007, T-I-021, T-I-022, T-I-023 verified 2026-08-08: T-I-021 and T-I-023 both passed vacuously before the migration existed (PostgREST's own `404 PGRST202` for a missing function coincided with the expected status), flagged explicitly rather than treated as real green; both got dedicated mutations post-migration (`errcode` changed, and `security definer` swapped in) that reddened only the intended test. T-A-007's own grant-revoke mutation surfaced a real bug (`PUBLIC`'s default `EXECUTE` grant masking the intended `authenticated`-only surface), fixed before the mutation was re-run and confirmed to redden all four tests. T-I-022 verified by removing the missing-variable guard entirely. Integration rows can return `429` from Supabase auth if the full suite is run several times in a minute — a rate limit on the test account, not a defect; space the runs. SL-011's T-A-006, T-I-017, T-I-018, T-U-027 verified 2026-08-08: T-A-006 and T-I-018 by revoking the `is_active` update grant (both reddened); T-I-018 additionally by opening the `owner_all` RLS policy (only it reddened among the three archive tests, discriminating the RLS mechanism specifically); T-I-017 by mutating its own assertion (no new mechanism on that call path — `prompt_title_unique` is already covered by T-I-004); T-U-027 by a no-op mutation of `filterByActive` itself. SL-005's T-I-019, T-I-020, T-U-028 verified 2026-08-08: both configuration tests reddened when the `select` grant was revoked; T-I-020 alone reddened a second time when `owner_select` was opened to `using (true)`, discriminating the RLS predicate from the grant; T-U-028 reddened when `applyConfigValues`'s presence guard was removed.
- [x] Quarantine empty.
