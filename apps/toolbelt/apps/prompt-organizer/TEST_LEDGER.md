# Test Ledger

A running record of this app's test suites: what exists, what it covers, and the result of its most recent run. Historical context for contributors, not a merge gate — `Toolbelt` is the gate.

The detailed, per-test rationale that used to live here (`specs/TEST-LEDGER.md`) is retired; that reasoning is preserved in git history and, going forward, in commit messages.

| Suite | Covers | Command | Last run | Result |
| --- | --- | --- | --- | --- |
| `tests/*.test.mjs` | `prompt` schema: list/search, versions, variables/render, tags, usage tracking, archive | `node --test "backend/tests/*.test.mjs" "frontend/tests/*.test.mjs"` | 2026-08-13 | pass (85/85; calls the live Supabase project, plus real-Postgres suites below) |
| Playwright critical browser journey | End-to-end UI flow against a served `frontend/` | `PLAYWRIGHT_BASE_URL=http://localhost:8812 npx playwright test --config frontend/playwright.config.mjs` | 2026-08-20 | pass (issue #249: now asserts the save settled and reads the row back over REST; teardown moved to an always-run `afterEach`) |
| `tests/migration-contract.test.mjs` | Offline migration guard: atomic retention claim/accounting, purge ACL, scoped-agent RPC-only grants, conditional cache-source shape, `p_values` shape, seed rollback provenance, and the CI Postgres contract | `node --test backend/tests/migration-contract.test.mjs` | 2026-08-13 | pass (5/5) |
| `tests/get-prompt.test.mjs` (m4-03/m4-04) | `prompt.get_prompt` rendering plus `prompt.get_prompt_source` atomic/conditional cache reads: pinned-vs-latest resolution, archival, PT404/PT422 conditions, merge order, owner authorization, scoped `prompt_get_agent` execute-only access, and down migrations. Real PostgreSQL; these RPCs are not deployed live yet. | `PROMPT_REQUIRE_POSTGRES=1 PROMPT_TEST_DATABASE_URL=postgresql://... node --test backend/tests/get-prompt.test.mjs` | 2026-08-13 | not run locally (no PostgreSQL server; 21 cases reported skipped). CI is configured to fail, not skip, when `PROMPT_REQUIRE_POSTGRES=1`. |
| `tests/seed.test.mjs` (m4-03) | Starter seeds: PO-4 coverage, Idea Intake contract, idempotency, stable migration-owned IDs, pre-existing-title survival through rollback, cascade, and non-superuser-owner deployability. Real PostgreSQL. | `PROMPT_REQUIRE_POSTGRES=1 PROMPT_TEST_DATABASE_URL=postgresql://... node --test backend/tests/seed.test.mjs` | 2026-08-12 | not run locally (no PostgreSQL server; 8 cases reported skipped) |
| `tests/retention.test.mjs` | `purge_old_usage`: no API-role EXECUTE, owner/cron execution, old-row aggregation/deletion, recent-row preservation, idempotent follow-up, and exact down behavior. Real PostgreSQL with a minimal pg_cron registration stub. | `PROMPT_REQUIRE_POSTGRES=1 PROMPT_TEST_DATABASE_URL=postgresql://... node --test backend/tests/retention.test.mjs` | 2026-08-12 | not run locally (no PostgreSQL server; 3 cases reported skipped) |
| `tests/performance.test.mjs` extension (m4-03) | PO-2: `rpc/get_prompt` p95 over 50 warm calls under 150ms, engine-level against real PostgreSQL; network/PostgREST remains a separate post-deploy measurement. | `PROMPT_REQUIRE_POSTGRES=1 PROMPT_TEST_DATABASE_URL=postgresql://... node --test backend/tests/performance.test.mjs` | 2026-08-12 | shared fail-on-skip harness wired; not re-run locally because PostgreSQL is unavailable |

Add a row when a new suite is introduced. Update a row's "Last run"/"Result" when it materially changes — this is not meant to be updated on every CI run.
