# Repository metadata. Fill in the values for this repository; delete fields
# that don't apply rather than leaving placeholders.

project:
  name: <repo-name>
  type: <one line: what kind of thing this repository builds>

ci:
  workflow: .github/workflows/ci.yml
  required_check: "PR Gate"

work_tracking:
  system: github-issues

standard:
  repo: kgsmith19/agent-engineering-standard
  lock_file: standard.lock
