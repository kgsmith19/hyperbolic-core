{
  "id": "brain",
  "name": "The Brain",
  "kind": "headless",
  "version": "0.1.0",
  "description": "07-brain-architecture.md: a long-lived autonomous-coding orchestrator (daemon, SQLite WAL state store, DAG scheduler, crash recovery, brain.task.v1/brain.result.v1 contracts, ACC-kernel-subprocess harness adapters, autonomy/approval policy, the `brain` CLI, and an authenticated HTTP API -- owner-session + agent-token auth, run/task CRUD, lossless SSE replay, all sharing one service layer. UI lands in a later M4 issue. Holds its own isolated Anthropic key (ADR-05); cannot read /platform/llm/.",
  "ownership": {
    "owner": "kylegsmith19@gmail.com",
    "path": "services/brain"
  },
  "entry": {
    "headless": {
      "command": "node src/index.ts"
    }
  },
  "schemas": [],
  "permissions": {
    "db": {
      "read": [
        "core"
      ],
      "write": []
    },
    "networkEgress": [
      "api.anthropic.com",
      "github.com"
    ],
    "llmHandler": {
      "access": false
    }
  },
  "lifecycle": {
    "migrate": "gh workflow run platform-migrations.yml",
    "health": "npm test",
    "register": "20260814160000_register_brain.sql"
  }
}
