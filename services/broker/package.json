{
  "name": "@hyperbolic/broker",
  "version": "0.0.0",
  "private": true,
  "description": "The guards broker (Epic #182, issue #185): a log-only pass-through forward proxy in this phase -- logs every proxied request (caller, target host, timestamp) and forwards it unmodified. No credential injection (#186), no egress enforcement (#187), no budget enforcement (#188) yet.",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "test": "node --test tests/*.test.ts",
    "typecheck": "tsc -b . && tsc -p tsconfig.test.json"
  },
  "dependencies": {
    "@hyperbolic/broker-contract": "*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.9.3"
  }
}
