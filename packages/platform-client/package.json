{
  "name": "@hyperbolic/platform-client",
  "version": "0.0.0",
  "private": true,
  "description": "Shared platform session client: Supabase Auth-backed sign-in and authed fetch for every hyperbolic-core zone (ADR-03).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --test tests/",
    "typecheck": "tsc -b . && tsc -p tsconfig.test.json"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
