{
  "name": "@hyperbolic/llm",
  "version": "0.0.0",
  "private": true,
  "description": "Provider-agnostic LLM abstraction (request/response/streaming/tool-use normalization, error taxonomy, retry/backoff). Zero key handling: callers pass credentials in per call. Anthropic driver only; OpenAI/Gemini land in m4-02.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --test tests/",
    "typecheck": "tsc -b ."
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.116.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
