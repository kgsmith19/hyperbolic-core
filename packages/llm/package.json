{
  "name": "@hyperbolic/llm",
  "version": "0.0.0",
  "private": true,
  "description": "Provider-agnostic LLM abstraction (request/response/streaming/tool-use normalization, error taxonomy, retry/backoff). Zero key handling: callers pass credentials in per call. Anthropic, OpenAI, and Gemini drivers.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --test tests/*.test.ts tests/*.test.mjs",
    "typecheck": "tsc -b . && tsc -p tsconfig.test.json"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.116.0",
    "@google/genai": "^2.16.0",
    "openai": "^7.4.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
