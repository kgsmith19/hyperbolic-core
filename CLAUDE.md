# CLAUDE.md

@AGENTS.md

This file exists only because Claude Code looks for it by name. The operational map lives in `AGENTS.md` above -- edit that file, not this one, so every agent (not just Claude Code) reads the same instructions.
