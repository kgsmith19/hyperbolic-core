"""Intentions domain (roadmap INT1): the operator's priority list as registry
data — the at-most-three focus goals, their floors and next physical actions.

Types are registry data (zero kernel DDL, invariant 1); every state change
goes through kernel application services (invariant 7). Display-only by
decision: no triggers, no scheduler, no recurrence, no completion analytics —
E1 owns all of those.
"""
