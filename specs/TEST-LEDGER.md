---
title: Test Justification Ledger
status: active
created: 2026-08-07
updated: 2026-08-09
owner: Kyle Smith
---

# Test Justification Ledger

> **Purpose.** Every test in this repo earns its keep by catching a specific failure. Coverage is not a reason; "it might break someday" is not a reason.
>
> **Adoption note:** this repo's ~445 existing tests predate this ledger and are not backfilled here row by row — that would be a multi-day mechanical exercise with no functional value, since their justification already lives where it's more durable: in `kernel/README.md`, `AGENTS.md`'s "The regression, exactly" section, and the git history of the bugs each one was written to catch (the 12-module kernel scenario-enumeration pass, closed as former `OPEN-ISSUES.md` OI-019, is the single largest example). **Section 1 starts empty and is required for every NEW test from here forward** (`rules/06-TESTS.md` GATE-TEST-JUSTIFIED). Section 3 (regression register) is seeded with the highest-value historical regressions so their "which gate missed it" lesson isn't lost.

---

## 1. Active tests

| Test ID | Name / location | Level | Traces to | Failure mode caught | Why not cheaper | Why not duplicate | Mutation verified | Runtime (ms) | Deletion criterion | Added |
|---|---|---|---|---|---|---|---|---|---|---|
| T-I-001 | `hooks/directive.test.mjs`: "issue #14: N concurrent appendCycle processes lose no update" | integration (real subprocesses) | FR-011 (directive survives resets — a lost update stalls the loop silently) | Two concurrent hook processes each read-modify-write the same directive file; one side's update is silently lost, which presents as the exact silent stall the directive loop exists to prevent | A type/lint rule cannot see a cross-process race; only a real multi-process test exercises the file lock | No other test runs concurrent real processes against the directive store | 2026-08-07 (lock neutered via a temporary seam → test went red, 48/49; restored → green ×3) | ~400 | When the directive store moves off per-file JSON (the SPEC-0005 demolition did NOT meet this: Stop-hook and runner appendCycle still race cross-process) | this change |
| T-U-002 | `runner/runner.test.mjs`: AC-001…AC-003, PROP-001 (directive-job synthesis + refusals) | unit | FR-011 | A run starting against a done/blocked/missing directive, or one with no working folder — headless token spend against work nobody asked for | Refusal is a runtime decision over live store state; no static check sees it | First tests ever touching `loadDirectiveJob` | 2026-08-07 red-first: all failed before the implementation existed (12-red run recorded in the PR) | <10 | Dies only if directive-backed jobs are removed | SPEC-0001 |
| T-U-003 | `runner/runner.test.mjs` + `hooks/directive.test.mjs`: directiveState/lastCycleBody timestamp exclusion | unit | FR-011 | Cycle headers carry timestamps; hashing them makes every run read as "progress", silently disarming the stuck brake — the runaway-loop failure that burns maxRuns×180min of tokens | The hash-input choice is semantic; only a test pinning "identical bodies compare equal" holds it | The file-job hash tests never exercise the log format | 2026-08-07 red-first | <10 | Dies with the directive log format | SPEC-0001 |
| T-I-002 | `runner/runner.test.mjs`: AC-004 (ACC_DIRECTIVE env isolation, real spawn) | integration (fake claude) | FR-011 | A file job's child inheriting a directive identity would adopt and mutate a directive it was never launched for (the OI-006 hijack class, headless edition) | Env passthrough is real-spawn behavior; only a subprocess proves it | Only test asserting the env var on the real spawn path | 2026-08-07 red-first | ~600 | Dies with directive jobs | SPEC-0001 |
| T-U-004 | `runner/runner.test.mjs`: AC-008 (red tier holds the loop) + liveTier fail-shapes | unit | FR-005 | The headless path spending tokens straight through a red week — FR-005's brake silently absent from the new transport | Tier is a runtime read; injectable fn makes each failure shape deterministic | nothing else gates the runner | 2026-08-07 red-first | <10 | When FR-005's thresholds mechanism is redesigned | SPEC-0001 |
| T-U-005 | `runner/runner.test.mjs`: AC-005/AC-007/PROP-002 (done detection, one cycle per run) | unit | FR-011 | A loop that never notices `done` (runs forever) or double-appends cycles (corrupts the continuity log the next fresh context reads) | Loop behavior over injected runs; milliseconds, no spawn | Only tests of runLoop's directive branch | 2026-08-07 red-first | <10 | Dies with directive jobs | SPEC-0001 |
| T-I-003 | `gui/server.test.mjs`: guards API group (AC-001…AC-009 + error paths, fake engine) | integration (real HTTP, fake engine subprocess) | FR-010, NFR-001 | An unallowlisted verb or malformed/traversal-shaped browser input reaching `engine.mjs`; CSRF against the localhost mutator; an engine failure masked as success | The allowlist and validation are runtime request handling; only real HTTP requests prove the refusals refuse | First tests of the guards API; kernel-policy tests cover a different route | 2026-08-07 red-first (11 red before the routes existed) | ~1.5s | Dies if the web GUI is retired (reversal of ADR-0002/0004) | SPEC-0002 |
| T-E-001 | `gui/e2e/guards.spec.mjs` (3 specs, Playwright, fake engine) | e2e | FR-010, NFR-008 | The page rendering stale state after a mutation, or wiring a button to the wrong verb — invisible to API-level tests, exactly the WinForms GUI's historical un-testability this migration exists to end | Only a real browser proves DOM↔API wiring | API tests cannot see the DOM | UI wiring: a mis-wired id fails the locator | ~2s | Dies with the guards page | SPEC-0002 |
| T-I-004 | `gui/server.test.mjs`: vault API group (AC-001…AC-007, PROP-001/002) | integration (real HTTP, fake engine records stdin) | FR-010, NFR-001 | A secret value leaking into argv (process listing/log), a response body, or a forged extra vault line via a `\n` in a value or an `=`/reserved name in a key — the exact reasons SPEC-0002 deferred this surface | The stdin-only channel and the framing-injection defenses are runtime request handling; only a test recording the child's stdin proves the value never went anywhere else | The other guards routes carry no secret value | 2026-08-07 red-first (8 red before the routes) | ~0.5s | Dies if the vault moves off the by-name engine model | SPEC-0003 |
| T-E-002 | `gui/e2e/guards.spec.mjs` vault specs (2, Playwright) | e2e | FR-010, NFR-001 | The value lingering in the DOM after a save (readable by any later script), or the name list not reflecting the store | Only a real browser proves the input is cleared and the DOM never holds the value | API tests cannot see the DOM lifecycle | asserting `#vaultInput` empties + the secret absent from persisted state | ~1s | Dies with the vault tab | SPEC-0003 |
| T-I-005 | `gui/server.test.mjs`: process API group (AC-001…AC-010, PROP-001/002) | integration (real HTTP, fake usage/budget, sandbox policy) | FR-010, NFR-006, NFR-007 | A dials save corrupting an unowned policy block (kernel/rates), a control action reaching an arbitrary command, or an invalid dial partially writing policy.json | The merge-preservation and the allowlist are runtime request handling; only real requests against a real policy file prove them | Other GUI routes don't touch policy.json or process control | 2026-08-07 (mutation-verified: dropping `...policy` reddened the preservation test; an inherited-key allowlist bypass reddened AC-009) | ~0.5s | Dies if the spending tab leaves web | SPEC-0004 |
| T-E-003 | `gui/e2e/guards.spec.mjs` spending specs (3, Playwright) | e2e | FR-010, NFR-006, NFR-008 | The tier/dials rendering stale, a dials save not persisting or clobbering the kernel block, or STOP/Resume not wired to their side effects | Only a real browser proves the DOM↔API↔policy.json round trip | API tests can't see the rendered tier or the button wiring | UI wiring + policy round-trip asserted against the real file | ~2s | Dies with the spending tab | SPEC-0004 |
| T-U-007 | `runner/runner.test.mjs`: pid-file singleton group (5 tests: live-pid refusal exit 6, stale/garbage reclaim, pid present during run + released after, lost reclaim race refuses without throwing, every early-exit path releases) | unit | FR-011, FR-012 (PROP-103/104) | Two runner loops on one directive — one accidental web double-launch away — double-spending tokens and interleaving cycle appends; or a crashed loop's stale pid file bricking a directive forever | The claim is cross-process filesystem state (wx-create, liveness probe); no static check sees it | Nothing else exercises runLoop's entry claim; lane/shim caps gate *spawns*, not loops | 2026-08-08 red-first (3 fail recorded before the implementation) | ~50 | Dies if the runner's loop model is replaced | SPEC-0005 |
| T-I-006 | `gui/server.test.mjs`: launch API group (AC-101…AC-113, real store/router/lane in sandbox, fake runner) | integration (real HTTP + real subprocess owners) | FR-012, NFR-001 | A traversal-shaped id reaching the filesystem; a refused create/status mutating the store; multi-line text mangled through argv; a launch spawning the wrong argv or double-launching past a live loop; profiles leaking `_note` | Request validation + child argv construction are runtime request handling; only real HTTP against the real store proves the refusals refuse and the store round-trips | The guards/vault/process groups never touch the directive store, router, lane, or runner spawn | 2026-08-08 red-first (13 fail recorded before the routes existed) | ~2s | Dies if the launch surface leaves the web (reversal of ADR-0006) | SPEC-0005 |
| T-I-007 | `gui/server.test.mjs`: ui-dist group (6 tests: serve/SPA-fallback/content-types, built-ins + API unshadowed, traversal shapes raw/encoded/backslash, broken-dist 500, unset-identical, CLI flag) | integration (real HTTP, fixture dist) | FR-012, NFR-001 (SPEC-0006 PROP-301) | The server's FIRST request-derived filesystem path escaping the dist root — a crafted `/../` (raw, URL-encoded, or backslash) reading policy.json/vault-adjacent files over HTTP; or the dist shadowing `/api`/built-ins | Path containment is runtime request handling; only real requests against a real tree prove the resolved-path check | No other test touches a request-derived fs path — the PAGES map is exact-match by design | 2026-08-08 red-first (feature test red before implementation; traversal pins arm with it) | ~0.3s | Dies if `--ui-dist` serving is removed (reversal of ADR-0006) | SPEC-0006 |
| T-E-004 | `gui/e2e/start-work.spec.mjs` (5 specs, Playwright, fake runner) | e2e | FR-012, NFR-008 | The GO flow mis-wired end to end: suggestion not filling the folder, the profile radio not landing on the directive, Mark-finished not archiving, or the log tail not live-updating — none visible to API tests | Only a real browser proves textarea→blur→suggest→GO→list→buttons wiring | T-I-006 cannot see the DOM | UI wiring: mis-wired ids fail locators; the fake runner's argv record proves the launch left the page | ~9s | Dies with the built-in guards page (ADR-0006 parity criterion) | SPEC-0005 |
| T-U-008 | `hooks/directive.test.mjs`: doneWhen round-trip/validation/backward-compatibility group | unit | FR-014 | Invalid done conditions (non-string, blank, multiline, oversize) entering store state; or optional omission becoming a synthetic default; or CLI `new` dropping the field | Validation and persistence are local pure/store logic; unit tests are the cheapest proof | Existing directive tests covered only task text and budget fields, not doneWhen semantics | 2026-08-09 (mutation verified by temporarily removing doneWhen checks/field write → tests red) | <10 | Dies if directive metadata moves away from this store/CLI path | this change |
| T-I-008 | `hooks/budget.test.mjs`: SessionStart doneWhen injection + legacy-missing-field safety | integration | FR-014 | A fresh context silently dropping a directive's finish condition, or throwing/poisoning injection for archived legacy directives missing the field | Hook output is runtime assembly of store+env state, so integration is required | Directive-store tests do not execute SessionStart injection | 2026-08-09 red-first | ~20 | Dies if SessionStart stops injecting directive context | this change |
| T-I-009 | `gui/server.test.mjs`: `/api/directives` doneWhen validation and exact round-trip | integration (real HTTP + real directive store) | FR-014, NFR-001 | Browser create accepting multiline/oversize/blank doneWhen values, or altering a valid value before storage/response | Request validation + child argv construction are runtime request handling | Existing launch API tests did not exercise doneWhen contract | 2026-08-09 red-first | ~60 | Dies if create leaves the web API surface | this change |
| T-E-005 | `gui/e2e/start-work.spec.mjs`: doneWhen browser→store→SessionStart context flow | e2e | FR-014, NFR-008 | Start-work UI collecting doneWhen but dropping it before storage or before injected launch context | Only a real page plus real hook execution proves DOM wiring and launch-context continuity together | API/unit tests cannot prove browser field wiring | 2026-08-09 red-first | ~200 | Dies with built-in start-work page retirement | this change |
| T-U-009 | `hooks/directive.test.mjs`: tag normalization/idempotence + invalid-input refusal + legacy-tag fallback | unit | FR-013 | Tags stored with duplicates/unsafe values, or legacy directives surfacing `null`/missing tags, which breaks filtering and can reopen prototype-key risk | Tag grammar/normalization are pure logic owned by `hooks/directive.mjs`; unit tests are the cheapest complete check | Existing directive tests covered store lifecycle but not tag invariants | 2026-08-09 red-first (tests fail before tag helpers/validation paths exist) | <10ms | Dies if directives stop carrying tags | FR-013 |
| T-I-010 | `gui/server.test.mjs`: launch-tag additions (auto route-tag, create refusals for bad tags, legacy list `tags:[]`) | integration (real HTTP, sandboxed real store/router) | FR-013, NFR-001 | Browser-supplied tag payloads bypassing validation, route tag not being inserted, or legacy entries returning null tags from list API | Route verdict and request validation happen on HTTP path composition; integration tests are required to prove shell args + responses | Unit tests cannot prove `/api/directives` request/response shaping | 2026-08-09 red-first | ~0.3s | Dies if launch/directive API leaves this server | FR-013 |
| T-E-006 | `gui/e2e/start-work.spec.mjs`: inclusive tag filter + clear restore | e2e | FR-013, NFR-008 | Tag filter UI wired incorrectly (select does nothing, clear does not restore, wrong directives hidden) despite API correctness | Only a browser test can verify DOM filter controls and visible list transitions | API/integration tests cannot assert rendered filtering behavior | 2026-08-09 red-first | ~2s | Dies with built-in guards page retirement | FR-013 |
| T-U-010 | `hooks/engine.test.mjs`: full command suite (status/toggle/secret-protected lists/projects/list/trash-list, resolveRef shapes, run/trash/restore/flush runbox lifecycle, vault import/rm/keys/apply, unknown command) + a small real-subprocess wrapper group | unit (direct `main({argv, io})` import) + a few real-subprocess proofs | NFR-001 (guard/vault machinery must be provably correct — issue #75) | Was the only file in `hooks/`/`kernel/` with zero test coverage: a vault-apply regression writing a secret to the wrong key, a runbox script silently not archiving/restoring, or `flush` running without `--really`, all shipping unnoticed | Vault/runbox mutation is real filesystem state (BOM handling, stamped trash names, ambiguous-ref resolution); a type check can't see any of it, and a subprocess-only suite can't give covgate visibility into the lines it exercises | First tests of this file entirely | 2026-08-10 red-first (engine.mjs had no `ACC_ROOT`/injected-`io` seam to import against until this change added one; every assertion here was unrunnable before that plumbing existed) | ~2s | Dies if the guard engine's CLI contract is replaced | this change |

## 2. Level distribution

Not tracked per-level yet for the existing suite (~498 fast-tier tests across `hooks/`, `kernel/`, `gui/`, `runner/`, plus three Windows-only PowerShell suites). Populate at the first Test Review that adopts this ledger for new work.

## 3. Regression register

The highest-value historical regressions — each found a real bug, not a hypothetical one. Full detail lives in git history and `AGENTS.md`; this table is the index.

| Test ID | Defect | Date found | How it reached the code | Root cause | Fixed in | Also fixed by a cheaper mechanism? |
|---|---|---|---|---|---|---|
| T-R-001 | `kernel/guard.mjs`'s path check did a raw string-prefix match with no `..`-segment resolution — a crafted `writeRoots`-adjacent path bypassed the deny boundary | 2026-08-04 | No scenario-enumeration pass existed yet for the kernel's deny-by-default boundary | Missing `path.posix.normalize` before the prefix comparison | `kernel/guard.mjs`, `kernel/guard.test.mjs` (4 new tests) | Yes — `norm()` now normalizes before comparing, a pure-function fix, not just a test |
| T-R-002 | `guardhook.mjs`'s tool-call ceiling check was a TOCTOU race — concurrent tool-call fires could all read the same "attempts so far" and all pass a ceiling meant to allow one more | 2026-08-06 | No concurrent-fire scenario had been tested | Read-then-append with no synchronization across processes | `kernel/ledger.mjs`'s `withDecisionLock`, `kernel/guardhook.test.mjs` (60 concurrent fires) | Yes — a cross-process file lock, not just a test |
| T-R-003 | `kernel/run.mjs`'s supervisor tick had no try/catch around `readState()` — a harness fault crashed the whole kernel process, not just the one run | 2026-08-06 | No fault-injection test existed for the tick path | A timer callback isn't covered by `runTask`'s own async try/catch | `kernel/run.mjs`, `kernel/run.test.mjs` | Yes — the tick is now wrapped, the test proves the wrap |
| T-R-004 | `hooks/directive.mjs`'s `bindSession` rebound an active directive to any UUID-shaped session id from a hand-piped SessionStart payload, hijacking a live session's directive | 2026-08-03 (observed live) | A hand-run hook against live state, not a sandboxed test | No validation that the incoming id was UUID-shaped before rebinding | `hooks/directive.mjs`, `hooks/directive.test.mjs` | Yes — the UUID-shape check is the fix, not just a test |

**Still open, no test yet (tracked as GitHub issues, not ledger rows, since no fix has landed):** issue #13 (pipe/request-channel auth), issue #15 (missing per-directive hard ceiling). Issue #14 (unlocked directive-store writes) is fixed and covered by T-I-001 in section 1.

## 4. Deleted tests

| Test ID | Name | Deleted | Reason | Replaced by |
|---|---|---|---|---|
| — | `kernel/credentials.test.mjs`: `"vaultNames returns names and never values"` | 2026-08-07 | Tested a zero-caller export (`vaultNames()`) removed in the same pass; its one genuinely useful assertion (missing vault file yields no keys) is preserved via `envForKeys([])` | (adjacent existing test, adjusted) |
| — | `kernel/adapters/claude-code.test.mjs`: `"the handle's own stop() convenience method calls stopTask"` | 2026-08-07 | Tested a zero-caller convenience method (`handle.stop`) removed in the same pass; `stopTask` itself is still called directly by `run.mjs` and still covered | nothing needed — the real code path is `run.mjs` calling `stopTask` directly |
| — | `hooks/prompts.test.mjs` (whole file) | 2026-08-07 | Tested `hooks/prompts.mjs`, a module with zero callers anywhere in the repo (not wired into any hook, the GUI, or the CLI) — speculative infrastructure never adopted | nothing needed |
| T-U-001 | `hooks/directive.test.mjs`: swallowed-kick re-arm | 2026-08-08 | Its own deletion criterion met: "dies with the keystroke mechanism" (SPEC-0005 PR-2 / ADR-0005 deleted kicks entirely) | nothing needed — no kick exists to swallow |
| T-U-006 | `hooks/directive.test.mjs`: KICK_TEXT pin | 2026-08-08 | Criterion met: "dies with the kick mechanism". The constant itself survives as the runner bootstrap and stays pinned by T-U-002 (`runner/runner.test.mjs` AC-001) | T-U-002 |
| — | `hooks/clearbot.test.mjs` (whole file, 21 tests) | 2026-08-08 | Tested `watcher/clearbot.ps1`'s decision helpers — the watcher was deleted (ADR-0005) | nothing needed |
| — | `gui/guards-gui.test.mjs` (whole file) | 2026-08-08 | Tested the WinForms interactive-lane handshake — `guards-gui.ps1` was deleted; the web launch path's lane behavior is the runner's own (`withLaunchSlot`), already covered | T-I-006 (launch surface), lane.test.mjs |
| — | `gui/ptyhost.test.ps1` + `gui/ptyhost.e2e.ps1` | 2026-08-08 | Tested `Acc.PtyHost`/ConPTY — deleted with the embedded terminal | nothing needed |
| — | `e2e/loop.e2e.mjs` (proof tier, 5 scenarios) | 2026-08-08 | Every scenario exercised the keystroke/auto-cd/pty loop, all deleted; its token-spending proof role passes to the re-targeted SL-008 watched real run (`runner.mjs directive:<id>`) | SL-008 + `kernel/kernel.e2e.mjs` |
| — | `hooks/budget.test.mjs`: kick re-arm/human-classification pair, heartbeat-revive trio, ACC_PTY window + ptyAnchorPid trio | 2026-08-08 | Each tested a mechanism deleted wholesale (Stop-hook kick re-arm, clearbot revive, pty window capture); the reworded suite pins the replacement behavior (no clear-requests ever, runner-resume messaging, no-window SessionStart) red-first | reworded budget.test.mjs (8 assertions recorded red pre-surgery) |
| — | `hooks/directive.test.mjs`: pendingKicks group (6), recordTurnEnd group (6), OI-031 reap group (5), console-adoption pair, CLI pending/kicked | 2026-08-08 | All tested the console-PID/kick machinery deleted by SPEC-0005 PR-2; "active" now means "work to do" and stale actives are curated from the web list, so no reaper exists to test | store CRUD/locking/OI-006 rewords in the same file |
| — | `hooks/statusline.test.mjs`: heartbeat trio | 2026-08-08 | Tested `bot DEAD` — no watcher exists; replaced by one pin that the segment never renders | the never-renders pin |
| — | `hooks/route.test.mjs`: cd-request/deny/queued group (6) + replayable | 2026-08-08 | Tested the deny/auto-cd/queued-prompt channel, deleted; replaced by a never-blocks pin | the never-blocks pin |
| — | `gui/server.test.mjs`: AC-007 cleanup, AC-008 clear-now, two cleanup-on edge cases | 2026-08-08 | Tested control actions removed from the allowlist; AC-009 now proves those very strings are refused | AC-009 (extended, recorded red pre-surgery) |
| — | `hooks/lane.test.mjs`: categories group (6), interactive-breaker warn, try-acquire/reown/release CLI group (7) | 2026-08-08 | The interactive slot category's only caller was the WinForms GUI (deleted 2026-08-08); the category, its dials, and the CLI handshake were removed in the leanness pass — zero callers. The jam regressions (slots/pacing/jitter OI-018/breaker/transport classification) all live in the automation-lane tests, which stay | automation-lane suite + a compact runCli status test |
| — | `gui/e2e/kernel-settings.spec.mjs`: CSRF request-fixture spec | 2026-08-08 | Used Playwright's raw request fixture — no page, no DOM — duplicating `gui/server.test.mjs`'s "CSRF is closed by construction" (which also covers Host spoofing via raw node:http). The e2e tier's own justification ("only a real browser proves…") did not apply | the unit CSRF test (OI-022 design guard survives there) |
| — | `gui/server.test.mjs`: vault "AC-007" success-shape test | 2026-08-08 | Never forced a failure (its own comment admitted it); its only assertion duplicated AC-001. The test that actually forces the engine failure now carries the AC-007 label | the relabeled failure test |
| — | `hooks/budget.test.mjs`: "no directive: an under-budget stop still does nothing at all" | 2026-08-08 | Byte-identical execution path to "under hard: stop passes silently" (no directive, ctx under hard, empty output) — the OI-011 pins are the latched tests, untouched | the surviving twin |
| — | `runner/runner.test.mjs`: killTree posix-dispatch real-process test | 2026-08-08 | The orphan-kill proof (found 2026-08-01) survives twice: the direct killTreePosix real-process test and the integration hang test; this third copy added only one line of dispatch, which the win32 twin already proves by injection | killTreePosix test + integration hang test |
| — | `kernel/guardhook.test.mjs`: absent-autonomy half of the fails-closed test | 2026-08-08 | beforeEach(stage) guarantees no autonomy file in the sequential-ceiling test too — the half was a duplicate; the corrupt-state half (unique) survives as its own test | the sequential tool-call-ceiling test |

## 4a. Strengthened tests (leanness Pass 1)

A fresh test-quality audit (Lens B, 2026-08-07) found 5 tests that would still pass if the function under test were replaced with a hardcoded return value. Rather than delete the coverage outright, each was rewritten to assert the real, independently-computed value (an "oracle" — reading the real file/directory the fallback is supposed to resolve to, same pattern `hooks/usage.test.mjs` already used) — the underlying fallback behavior is real and worth testing, the old assertion just didn't prove it.

| Test | Location | What changed |
|---|---|---|
| `kernelRoot falls back to the repo root when ACC_ROOT is unset` | `kernel/policy.test.mjs` | `path.isAbsolute(...)` → equals an independently-computed real repo root |
| `loadKernelPolicy falls back to the repo policy.json when ACC_POLICY is unset` | `kernel/policy.test.mjs` | `doesNotThrow` → asserts the loaded `harness` matches the real `policy.json`'s own value |
| `end-to-end: ACC_ROOT unset falls back to the real repo root` | `hooks/testplan.test.mjs` | Switched from a non-firing prompt (proved nothing landed) to a firing one; asserts the latch file lands under the real repo's `runner/state/`, cleaned up immediately |
| `POLICY() and LANE_DIR() fall back to their real defaults` | `hooks/lane.test.mjs` | `cfg.slots >= 1` → equals the real `policy.json`'s own `lane.slots` value |
| `acquireSlot works end to end with ACC_LANE_DIR genuinely unset` | `hooks/lane.test.mjs` | No assertion → reads the real `owner.json` under `os.tmpdir()/acc-lane/slot-N/`, and confirms `release()` actually removes it |

## 5. Quarantine

*(none)*

## 6. Ledger self-check (GATE-LEDGER)

- [ ] Every test file in the repo has a matching row in section 1. **Not yet true — see adoption note.**
- [x] Section 3's regressions are indexed with a real defect, root cause, and fix.
- [x] Section 4 records every deletion made in this cleanup pass with a reason.
- [ ] Quarantine has no expired entries. (empty, trivially true)
